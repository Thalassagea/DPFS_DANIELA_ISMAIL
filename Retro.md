# Retrospectiva - Sprint 8

##  Comenzar a hacer
-Planificar primero la estructura de la API (endpoints, JSON de respuesta, paginado) antes de implementar.
-Definir claramente la fuente de datos (base/JSON) y mantenerla única para evitar incoherencias.
-Documentar en el README el contrato de la API y cómo correr backend + dashboard.
##  Hacer más
-Reforzar validaciones backend y frontend (registro/login y creación/edición de cursos).
-Probar endpoints con diferentes escenarios (404, paginado, datos vacíos).
-Mantener consistencia de nombres (courses vs products) y rutas API alineadas a la consigna.

##  Continuar haciendo
-Mantener la organización del proyecto y estructura MVC clara.
-Usar el tablero (Trello/Notion) para ordenar tareas y dependencias.
-Trabajar con buenas prácticas en Express, middlewares y EJS para mantener código limpio.
-Separar el dashboard en componentes React reutilizables.

##  Hacer menos
-Corregir a último momento sin testeo previo.
-Cambiar rutas/estructura sin revisar impacto en la API y el dashboard.

##  Dejar de hacer
-Postergar el testeo de endpoints y dashboard hasta el final.
-Agregar funcionalidades fuera de consigna que desvían tiempo del objetivo principal.
